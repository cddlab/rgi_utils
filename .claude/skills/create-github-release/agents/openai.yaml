interface:
  display_name: "Create GitHub Release"
  short_description: "Create a verified version tag and GitHub Release"
  default_prompt: "Use $create-github-release to verify the current version, get my approval, and create its GitHub tag and Release."
